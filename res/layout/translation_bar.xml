<?xml version="1.0" encoding="utf-8"?>
<com.nhs.customkeyboard.translate.TranslationBarView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/translation_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/transparent"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

  <!-- Row 1: Back arrow + Language selection pills + Swap -->
  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="38dp"
      android:orientation="horizontal"
      android:gravity="center_vertical">

    <ImageButton
        android:id="@+id/btn_translate_back"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/bg_circle_button"
        android:src="@drawable/ic_arrow_back"
        android:scaleType="fitCenter"
        android:tint="?attr/colorLabel"
        android:padding="8dp"
        android:contentDescription="@android:string/cancel"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="horizontal">

      <!-- Source Language Pill -->
      <TextView
          android:id="@+id/btn_source_lang"
          android:layout_width="wrap_content"
          android:layout_height="34dp"
          android:minWidth="84dp"
          android:background="@drawable/bg_lang_pill"
          android:gravity="center"
          android:paddingStart="16dp"
          android:paddingEnd="16dp"
          android:text="English"
          android:textColor="?attr/colorLabelAction"
          android:textSize="13sp"
          android:textStyle="bold"
          android:clickable="true"
          android:focusable="true"/>

      <!-- Swap Icon -->
      <ImageView
          android:id="@+id/btn_swap_lang"
          android:layout_width="36dp"
          android:layout_height="36dp"
          android:layout_marginStart="8dp"
          android:layout_marginEnd="8dp"
          android:background="?android:attr/selectableItemBackgroundBorderless"
          android:src="@drawable/ic_swap_horiz"
          android:tint="?attr/colorLabel"
          android:scaleType="fitCenter"
          android:padding="8dp"
          android:clickable="true"
          android:focusable="true"/>

      <!-- Target Language Pill -->
      <TextView
          android:id="@+id/btn_target_lang"
          android:layout_width="wrap_content"
          android:layout_height="34dp"
          android:minWidth="84dp"
          android:background="@drawable/bg_lang_pill"
          android:gravity="center"
          android:paddingStart="16dp"
          android:paddingEnd="16dp"
          android:text="Bangla"
          android:textColor="?attr/colorLabelAction"
          android:textSize="13sp"
          android:textStyle="bold"
          android:clickable="true"
          android:focusable="true"/>
    </LinearLayout>
  </LinearLayout>

  <!-- Row 2: Rounded translation input box -->
  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_marginTop="6dp"
      android:layout_marginBottom="2dp"
      android:background="@drawable/bg_translate_input_box"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:paddingStart="14dp"
      android:paddingEnd="8dp">

    <TextView
        android:id="@+id/tv_translate_input"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical|start"
        android:hint="Type here to translate..."
        android:textColorHint="?attr/colorSubLabel"
        android:textColor="?attr/colorLabel"
        android:textSize="15sp"
        android:singleLine="true"
        android:ellipsize="start"/>

    <ImageView
        android:id="@+id/btn_translate_clear"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        android:tint="?attr/colorSubLabel"
        android:scaleType="fitCenter"
        android:padding="6dp"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true"/>
  </LinearLayout>
</com.nhs.customkeyboard.translate.TranslationBarView>
